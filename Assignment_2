Build a working prototype of a canteen menu card webpage. Use <div> sections for Header (canteen name/logo), Menu Categories (Breakfast, Lunch, Snacks, Beverages), and Footer (contact/timings). Display each category's items in a table (Item Name, Price, Availability), include images of featured dishes, and add an anchor tag linking to an "Order Now" or "Contact Us" section. Style it using a combination of internal and external CSS — apply borders to tables and images, distinct background colours for each menu category section, and consistent margin/padding for spacing.